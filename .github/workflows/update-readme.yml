name: Update Profile Grade and Age

on:
  schedule:
    # Runs at 00:00 UTC every single day to catch your birthday exactly on July 24th
    - cron: '0 0 * * *'
  workflow_dispatch: # Allows manual trigger

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run update script
        run: node update-readme.js

      - name: Commit and Push changes
        run: |
          git config --global user.name "Krylo-60"
          git config --global user.email "71krishivpb@gmail.com"
          git add README.md index.html
          if ! git diff-index --quiet HEAD --; then
            git commit -m "chore: auto-update age and academic grade for $(date +%Y-%m-%d)"
            git push
          else
            echo "No changes to commit"
          fi
